import CodeLanding from "@/components/code/CodeLanding";

// THROWAWAY SPIKE — the Code pillar landing. Two ways in (from your learnings /
// create your own), both opening the sample drill until the generator exists.
// No auth gate, no DB — self-contained like the rest of the Code spike.
export default function CodeStartPage() {
  return <CodeLanding />;
}
